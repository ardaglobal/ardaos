syntax = "proto3";
package ardaus.ardaus;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "ardaus/ardaus/params.proto";

option go_package = "arda-us/x/ardaus/types";

// GenesisState defines the ardaus module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
